# Polar Lab Report — 2026-07-16

**Audience:** Aileen (Mac) + operators  
**Repo:** https://github.com/lilaclilac09/polar-lab  
**Merged today:** PR [#6](https://github.com/lilaclilac09/polar-lab/pull/6) (short-fact v4), PR [#7](https://github.com/lilaclilac09/polar-lab/pull/7) (HF offline fallback)  
**Related:** [WHY_GOOD_WHY_BAD.md](WHY_GOOD_WHY_BAD.md) · [week_01.md](week_01.md) · [TODO.md](../TODO.md) · [NEXT_RUN.md](../docs/NEXT_RUN.md)

---

## 1. Bottom line (read this first)

| Question | Answer |
|----------|--------|
| Does the Polar Lab loop work? | **Yes** — data → LoRA SFT → holdout eval runs on CPU and Mac MPS |
| Did small Machina packs move the score? | **No** — stuck at **`exact_match = 0.200`** (only yes/no hits) |
| Did more short-fact data fix it? | **Yes on CPU** — v4 (450 train / 400 steps) → LoRA **`1.000`** vs base **`0.200`** |
| Did Mac finish v4 yet? | **Not yet** — pulled v4 OK, then **Hugging Face `ConnectTimeout`** before training |
| What should Mac do now? | `git pull` + `HF_HUB_OFFLINE=1` + `./run_next.sh` (model already cached from earlier download) |

**One sentence:** Polar Lab proved the pipeline; tiny data failed the holdout; hundreds of identical short answers made the adapter hit every holdout string on CPU; Mac is blocked only by Hub network, not by code or data.

---

## 2. Scoreboard

| Run | Device | Train rows | Steps | LoRA `exact_match` | Base | Notes |
|-----|--------|------------|------:|-------------------:|-----:|-------|
| Arithmetic demo | cpu | 10 | 40 | **0.667** | — | Smoke: short answers can stick |
| Machina v1–v2 | cpu | 24–36 | 40–120 | **0.125 → 0.200** | ~0.200 | Loss fell; facts still paraphrase/hallucinate |
| Machina v3 | cpu | 79 | 200 | **0.200** | 0.200 | Near-misses closer (`visitor:soft:memory`, `paradigm/centaur`) |
| **Mac first** | **mps** | **36** | 120 | **0.200** | **0.200** | Loss 5.1→1.56; same 2/10 yes/no hits |
| **CPU v4** | **cpu** | **450** | **400** | **1.000** | **0.200** | Δ **+0.800**; useful bar (≥0.60) **YES** |
| Mac v4 attempt | mps | 450 (pulled) | — | — | — | Died on HF tokenizer Hub timeout |

### Pass bars (unchanged)

| Gate | Bar | CPU v4 | Mac first (36-row) |
|------|-----|--------|--------------------|
| Beat old CPU baseline | > 0.200 | **YES** (1.000) | NO (0.200) |
| Useful | ≥ 0.60 | **YES** | NO |
| Clear win vs base | ≥ base + 0.20 | **YES** (+0.80) | NO (+0.00) |

---

## 3. What Mac already proved

From your terminal logs:

1. **Clone / pull / `./run_next.sh` works** on Apple Silicon.
2. **Device = `mps`**, dtype `float16`.
3. **First full run completed** on the old 36-row pack:
   - Train loss fell across 120 steps
   - Holdout: only  
     - “Should LoRA replace Markdown memory?” → `no`  
     - “Centaur default MCP?” → `no`  
   - Paths / Redis prefix / `paradigmxyz/centaur` / `NetworkPolicy` / `90` / `AGENTS.md` / `iron-proxy` / `sandbox pods` still wrong or verbose
4. **After PR #6:** `git pull` landed v4 (`train_rows: 450`, overlap 0) — correct pack.
5. **Then failed** with `httpx.ConnectTimeout` while `AutoTokenizer.from_pretrained` talked to Hugging Face (not a Polar Lab logic bug).

Ignore as noise: `pin_memory` MPS warning, `torch_dtype` deprecated, pip resume warnings, Chinese-comment `zsh: parse error near ')'`.

---

## 4. What CPU v4 proved (agent environment)

After expanding to **450** short-fact paraphrases with **identical golds** for the miss strings:

| Gold | LoRA prediction |
|------|-----------------|
| `aileena_second_brain/memories/semantic/` | exact hit |
| `visitor:soft:` | exact hit |
| `paradigmxyz/centaur` | exact hit |
| `NetworkPolicy` | exact hit |
| `90` | exact hit |
| `sandbox pods` | exact hit |
| `AGENTS.md` | exact hit |
| `iron-proxy` | exact hit |
| yes/no ×2 | exact hit |

Base model on the same holdout stayed at **0.200** (still invents / paraphrases).  
So the lift is the **LoRA adapter + data**, not a softer metric cheat.

Hygiene: `scripts/check_data.py` → `overlap: 0`.

---

## 5. Why small packs failed / why v4 worked

| Failure mode (≤79 rows) | Example | Fix in v4 |
|-------------------------|---------|-----------|
| Paraphrase | “In the Aileena memory store.” vs path gold | Dozens of “reply with only the path” → same gold |
| Near-miss | `paradigm/centaur` vs `paradigmxyz/centaur` | Many identical `org/repo` golds |
| Ignore “reply with only” | Long waffle on Redis / warm pool | Mass short-answer rows |
| Hallucinate names | `Gitlab-proxy`, `rewards.md` | Repeat `iron-proxy`, `AGENTS.md` |

**Lesson:** for `exact_match` on brittle strings, volume of *identical short answers* beats jumping to 1.5B first.

---

## 6. What shipped in git (relevant PRs)

| PR | What | Status |
|----|------|--------|
| #1–#5 | SPEC/CI, `run_next.sh`, auto report, WHY/TODO | Merged |
| **#6** | Short-fact **v4** (450 rows), `max_steps: 400` | **Merged** |
| **#7** | HF Hub retry + local-cache fallback + docs | **Merged** |

`main` tip after #7: commit `6eed1b8` (or later).

---

## 7. Mac — finish v4 (copy-paste)

Model weights were already downloaded in your earlier successful run. Prefer offline cache so Hub timeout cannot block you:

```bash
cd ~/polar-lab
git checkout main && git pull
export HF_HUB_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
./run_next.sh
open logs/LATEST_RUN_REPORT.md
```

Expect:

- Banner: `train_rows: 450`, `device: mps`
- ~400 training steps (several to ~15+ minutes)
- Report gates closer to CPU: LoRA ≫ 0.20 (CPU hit **1.000**)

If offline says cache missing, drop the two `export` lines and retry when Hugging Face is reachable (or set `HF_TOKEN`).

---

## 8. C / D / E checklist status

| Letter | Topic | Status |
|--------|--------|--------|
| **C** | Data first / score | **CPU done (1.000)**; Mac v4 confirm pending (HF timeout) |
| **D** | Hygiene | **OK** — overlap 0, `outputs/` gitignored |
| **E** | Codex CI | Docs ready; you still set `OPENAI_API_KEY` + `ENABLE_CODEX_CI=true` if wanted |

---

## 9. What “done” looks like for this week

1. Mac completes `./run_next.sh` on **v4** without Hub crash  
2. `logs/LATEST_RUN_REPORT.md` shows LoRA **≥ 0.60** (ideally near 1.0 like CPU)  
3. One row logged in `logs/week_01.md` for the Mac MPS v4 run  
4. Codex CI only if you want the optional inspector  

---

## 10. Links

- Repo: https://github.com/lilaclilac09/polar-lab  
- Auto report after each local run: `logs/LATEST_RUN_REPORT.md` (gitignored; local only)  
- Why analysis: [WHY_GOOD_WHY_BAD.md](WHY_GOOD_WHY_BAD.md)  
- Next-run paste path: [docs/NEXT_RUN.md](../docs/NEXT_RUN.md)  
- HF timeout note: same file, section “Hugging Face timeout”

---

*Generated 2026-07-16 for Mac + CPU results through PR #7 merge.*
